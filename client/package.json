{
  "name": "client",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npm run build:css && npm run build:js",
    "build:css": "NODE_ENV=production postcss ./src/kament.css -o dist/kament.css --verbose",
    "build:js": "node build.js"
  },
  "devDependencies": {
    "autoprefixer": "^10.2.4",
    "cssnano": "^4.1.10",
    "esbuild": "^0.8.42",
    "postcss": "^8.2.4",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.0.2"
  },
  "dependencies": {
    "date-fns": "^2.17.0",
    "dompurify": "^2.2.6",
    "markdown-to-jsx": "^7.1.1",
    "preact": "^10.5.12"
  }
}
